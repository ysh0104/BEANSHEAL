---
description: Push directly to main after task completion; do not open pull requests
alwaysApply: true
---

# Git Workflow — Direct Push to main

This project uses **direct push to `main`**. Do not create pull requests unless the user explicitly asks.

## Auto push (기본)

- **코드/설정 수정을 마치면 `main`에 커밋 후 `git push origin main`까지 자동 실행.**
- 사용자가 "푸쉬하지 마" / "커밋만" / "로컬에만"이라고 **명시적으로 막을 때만** push 생략.
- 커밋 메시지는 변경 목적(why) 중심으로 1~2문장.
- push 전 `git pull --rebase origin main`으로 원격 반영 후 push.

## Default branch

- Work on **`main`** only.
- Push with: `git push origin main`

## When committing and pushing

1. Confirm on `main` (`git status`, `git branch`).
2. If not on `main`, checkout `main` and merge/rebase local work first.
3. Stage relevant files, commit, pull --rebase, push to **`origin main`**.

## Do not

- Run `gh pr create` or suggest opening a PR by default.
- Push to `cursor/*` or other long-lived feature branches unless the user asks.
- Force-push `main` unless the user explicitly requests it.
- Commit secrets (`.env`, credentials) — warn if requested.

## If push is rejected (non-fast-forward)

1. `git pull origin main` (rebase) to integrate remote changes.
2. Resolve conflicts if any, commit, then `git push origin main`.
